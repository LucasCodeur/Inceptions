services:
  nginx:
    restart: always
    networks:
      - inceptions
    container_name: nginx
    ports:
      - "443:443"
    build: ./requirements/nginx 
    env_file:
      - .env
    volumes:
      - wordpress-volume:/var/www/html
    depends_on:
      - wordpress
  wordpress:
    restart: always
    networks:
      - inceptions
    container_name: wordpress
    build: ./requirements/wordpress
    env_file:
      - .env
    volumes:
      - wordpress-volume:/var/www/html
    depends_on:
      - mariadb
  mariadb:
     restart: always
     networks:
       - inceptions
     container_name: mariadb
     build: ./requirements/mariadb
     env_file:
       - .env
     volumes:
       - mariadb-volume:/var/lib/mysql
     environment:
       MYSQL_DATABASE: ${DB_NAME}
       MYSQL_USER: ${DB_USER}
       MYSQL_PASSWORD: ${PASSWORD}
       MYSQL_ROOT_PASSWORD: ${ROOT_PASS}

volumes:
  mariadb-volume:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: /home/lud-adam/data/mariadb-volume
  wordpress-volume:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: /home/lud-adam/data/wordpress-volume

networks:
  inceptions:
